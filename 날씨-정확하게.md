# 날씨를 네이버와 똑같이 맞추기 (기상청 API)

기본값은 Open-Meteo(해외 모델)라 기상청 예보와 몇 도씩 차이가 납니다. 무료 기상청 키를 넣으면 네이버가 쓰는 것과 **같은 기상청 단기예보**를 그대로 씁니다.

## 1. 키 발급 (5분, 무료)

1. `data.go.kr` 접속 → 회원가입 / 로그인
2. 검색창에 **단기예보** 입력 → **기상청_단기예보 ((구)_동네예보) 조회서비스** 선택
3. 우측 **활용신청** 클릭 → 활용목적은 아무거나(예: 개인 학습용) → 신청
   - 자동 승인이라 바로 쓸 수 있습니다
4. 상단 **마이페이지 → 개발계정 → 해당 API** 클릭
5. **일반 인증키** 복사 — Encoding·Decoding 어느 쪽이든 앱이 알아서 처리합니다

## 2. 앱에 넣기

설정 → **기상청 API 키 (선택)** 칸에 붙여넣고 저장.

날씨 카드 우측 상단이 `Open-Meteo`에서 **`기상청`**으로 바뀌면 성공입니다.

## 3. 위치 맞추기

설정에 **지역 이름 / 위도 / 경도** 칸이 있습니다. 기본값은 양천구 목동(37.5271, 126.8656).

- 다른 곳이면 **현재 위치로 맞추기** 버튼을 누르면 자동으로 채워집니다
- 위경도는 기상청 격자(5km)로 자동 변환됩니다 — 목동은 nx 58, ny 126

## 무엇이 어디서 오는지

| 항목 | 키 넣기 전 | 키 넣은 후 |
|---|---|---|
| 기온 · 최고/최저 · 강수확률 · 습도 · 하늘상태 | Open-Meteo | **기상청** |
| 자외선지수 · 일출/일몰 | Open-Meteo | Open-Meteo (기상청 단기예보에 없음) |
| 미세먼지 · 초미세먼지 | Open-Meteo 대기질 | 그대로 |

## 안 될 때

- 발급 직후 1시간 정도는 키가 활성화되지 않는 경우가 있습니다. 조금 뒤에 다시
- 날씨 카드 우측에 `Open-Meteo (기상청 실패)`가 뜨면 그 글씨를 눌러 보세요. 기상청이 돌려준 오류 메시지를 그대로 보여줍니다
- data.go.kr 대신 기상청 API 허브(apihub.kma.go.kr) 인증키를 넣어도 동작합니다
- 실패하면 자동으로 Open-Meteo 값으로 돌아가니 화면이 비진 않습니다
