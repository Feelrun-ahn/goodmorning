# 폰과 태블릿에서 같은 목록 보기 (클라우드 동기화)

**할 일 · 내일 할 일 · 수행평가 · D-day** 네 가지가 기기 간에 공유됩니다.
날씨·운세·라이온즈는 기기마다 따로 계산되고, 수면 점수는 워치를 확인한 기기에 직접 적는 값이라 동기화 대상이 아닙니다.

Firebase Firestore를 씁니다. LionLive 때 만든 프로젝트를 그대로 재사용해도 되고, 새로 만들어도 됩니다. 무료 한도 안에서 충분히 돌아갑니다.

---

## 1. Firestore 만들기

1. `console.firebase.google.com` → 프로젝트 선택(또는 새로 만들기)
2. 좌측 **빌드 → Firestore Database → 데이터베이스 만들기**
3. 위치는 `asia-northeast3 (서울)`, 모드는 **프로덕션 모드**로 시작

## 2. 규칙 넣기

Firestore → **규칙** 탭에 아래를 붙여넣고 게시:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /briefing/taehyun-goodmorning {
      allow read, write: if true;
    }
  }
}
```

`taehyun-goodmorning` 문서 하나만 열어 두는 규칙입니다. 데이터베이스의 다른 곳은 전부 잠겨 있어서, 남이 마음대로 새 문서를 만들거나 다른 데이터를 건드릴 수 없습니다.

다만 **이 코드를 아는 사람은 목록을 읽고 쓸 수 있습니다.** 들어가는 게 할 일과 수행평가 일정이라 크게 민감하진 않지만, 신경 쓰이면 뒤에 아무 글자나 몇 개 붙이세요 — 예를 들어 `taehyun-goodmorning-k7q2`. 그럴 땐 규칙의 문서 이름과 앱 설정의 동기화 코드를 **똑같이** 바꿔야 합니다.

## 3. 값 3개 확인

Firebase 콘솔 → **프로젝트 설정(톱니) → 일반** 하단 "내 앱"의 웹 앱 설정에서:

| 앱 설정 칸 | 가져올 값 |
|---|---|
| 동기화 프로젝트 ID | `projectId` (예: `morning-briefing-a1b2c`) |
| 동기화 API 키 | `apiKey` (`AIza...`로 시작) |
| 동기화 코드 | `taehyun-goodmorning` (이미 채워져 있음) |

웹 앱이 없으면 "앱 추가 → 웹"으로 하나 만들면 값이 나옵니다.

## 4. 두 기기에 똑같이 입력

동기화 코드는 이미 `taehyun-goodmorning`으로 채워져 있으니, 태블릿과 폰 각각에서 **프로젝트 ID와 API 키 두 칸만** 넣고 저장하면 끝입니다.
설정 창 아래 "상태: 동기화됨 · 07:42"가 뜨면 연결된 겁니다.

- 할 일이나 수행평가를 바꾸면 약 1초 뒤 자동 업로드
- 다른 기기는 45초마다 확인해서 반영
- 규칙은 마지막에 저장한 쪽이 이깁니다. 두 기기에서 동시에 고치지만 마세요

## 참고

- API 키는 비밀번호가 아니라 프로젝트 식별자입니다. 실제 접근 통제는 위 규칙이 합니다
- 규칙의 문서 이름과 앱의 동기화 코드가 한 글자라도 다르면 "동기화 실패"가 뜹니다
- 동기화 3칸을 비우면 기능이 꺼지고, 지금처럼 기기 안에만 저장됩니다
- 간단히 한 번만 넘기고 싶으면 수행평가 카드의 **폰으로 보내기** 버튼(링크 복사)을 그대로 쓰면 됩니다
